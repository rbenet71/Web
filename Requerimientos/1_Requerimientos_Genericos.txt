MODIFICACIONES App
Voy a pedir unas modificacion/es de la app, y es importante que primero me pidas las funciones que necesitas ver para hacer estas modificaciones. 
Una vez las tienes proponme que ficheros has de modificar y porque, y no añadas ninguna nueva información o funcionalidad sin que antes te de el visto bueno.
Importante yo no se programar en Javascript, por lo que se poco sobre codigo,
una vez te doy el visto bueno, me haces los cambios. Si la función a modificar es pequeña me pasas la función completa y me indicas a sustituir la función. Si la función es muy grande y hay varios cambios sobre ella, me la pasas también toda, pero si es un solo cambio o una parte continua, solo haré ese cambio, pero sobretodo me dices sustituir esta parte por esta otra. NUNCA me pases la función como si me la pasases completa, pero en medio del cóldigo pones mantener el codigo anterior.
Una vez propuestos los cambios a continuación pasame completa la nueva versión del fichero MD con la estrucutra de la app.
siempre pensar que se ha de traducir y los nombres de las variables en camelcase para no tener problemes.
No empieces a escribir codigo hasta que no te valide las explicaciones que me des.

NUEVA App

Quiero hacer una app PWA:
- que pueda funcionar offline
- que esté preparada para traducir a Castellano, CAtalan, Ingles y Frances
- que tenga un botón de ayuda. (en fichero HTML)
- que tenga un boton de instalarse, si no está instalado.
- Al pie : <span id="copyright-text">2026 © Copyright</span> <a href="https://rbenet71.github.io/Web/" target="_blank" id="copyright-link"> Roberto Benet - rbenet71@gmail.com - V_18_12_2026</a>

Como lo previsible es que se haga muy grande, deben hacer varios ficheros JS con prevision que cuando se hagan cambios sea facil hcaerlo por encontrarse en pocos ficheros. Por ejemplo un solo js para las traducciones. Lo ideal arhivos de 300 a 500 lineas.

los nombres de los ficheros siempre llevaran el nombre de la app, por ejemplo Crono_CRI_Manifest.json (styles, ws,manifest, app,..), incluso el html que no se ha de llamar index. es para cuando se tenga que modificar saber que app es, ya que tendremos muchas. Los iconos pictos, y demas recursos estaran en una carpeta que se llame assets, el resto de arhivos estaran en el directorio raiz.

Se ha de crear el fichero [nombre app]+"Estructura.md" en el que se explique como funciona toda la app que funciones tiene y todo aquello que sea necesario para hacer cambios en la app. En el se ha de indicar que siempre que se haga un cambio, recordar actualizar el fichero completo.

Todas las opciones elegidas se han de memorizar, para que cuando vuelvas a entrar, si por ejemplo seleccionaste un arhivo vuelva a aparecer.

La app se ha de actualizar automaticamente, por tanto que sea facil modificar la version de la app.

Opciones:

- mantener Pantalla Activa
    <!-- Video oculto para mantener pantalla activa -->
    <video id="keep-alive-video" style="display: none;" muted playsinline>
        <source src="https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4" type="video/mp4">
    </video>



sistema de traducciones

// ===========================================
// TRADUCCIONES MULTI-IDIOMA
// ===========================================
const translations = {
    es: {
        // Títulos generales
        appTitle: "Crono CRI - en Construcción",
        languagesLabel: "Idioma / Language",


    ca: en: fr: 


function updateModeContent(t) {
    // Selector de modo
    setTextIfExists('mode-selector-title', t.modeSelectorTitle);
    setTextIfExists('mode-salida-text', t.modeSalidaText);
    setTextIfExists('mode-llegadas-text', t.modeLlegadasText);
    
    // Contenido salidas
    setTextIfExists('audio-config-title', t.audioConfigTitle);
    setTextIfExists('beep-option-title', t.beepOptionTitle);
    setTextIfExists('voice-option-title', t.voiceOptionTitle);
    setTextIfExists('mute-option-title', t.muteOptionTitle);